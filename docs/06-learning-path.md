# 第六章 推荐学习路线

容量评估不是一门孤立的课，它建立在计算机系统、网络、数据库、性能分析和分布式系统之上。

学习顺序很重要。很多人一上来就学 Kubernetes、Kafka、微服务，其实是倒过来了。底层资源都不理解，学再多框架也只是知道一些名词。一个工程师如果不知道 CPU Load 和 CPU 使用率的区别，不知道 TCP 连接为什么会耗资源，不知道 MySQL Buffer Pool 如何影响查询，就很难真正理解容量。

我建议按七个阶段学习。

## 1. 第一阶段：计算机资源基础

先学 Linux 和操作系统资源。

必须理解：

1. CPU Load。
2. Memory。
3. IO。
4. Network。
5. epoll（Linux）/ kqueue（macOS）。
6. 文件系统。
7. Page Cache。
8. mmap。
9. 零拷贝。
10. 进程、线程、上下文切换。

这一阶段的目标不是背命令，而是建立资源直觉。你要知道一个请求进入服务器后，经历了哪些内核和用户态过程。

比如，一个 HTTP 请求到达服务器，并不是直接进入你的业务函数。它先经过网卡、中断、内核协议栈、socket buffer、epoll、应用线程或协程调度，然后才到业务代码。业务代码查询数据库时，又会进入网络、磁盘、数据库 Buffer Pool 和锁管理。任何一个环节都可能成为瓶颈。

推荐练习：

1. 用 `top`、`htop`、`vmstat`、`iostat`、`sar` 观察系统资源。
2. 用 `strace` 看一个进程做了哪些系统调用。
3. 用 `perf` 或火焰图看 CPU 热点。
4. 写一个简单 HTTP 服务，逐步增加并发，观察 CPU、内存和连接数。
5. 构造大文件读写，观察 Page Cache 对性能的影响。

常见坑：

1. 把 CPU Load 当成 CPU 使用率。
2. 看到内存使用高就以为内存泄漏，不理解 Page Cache。
3. 不区分磁盘吞吐和 IOPS。
4. 不理解上下文切换过多会拖垮系统。
5. 在容器里只看宿主机指标，不看 cgroup 限制。

推荐读：

1. 《性能之巅》（Brendan Gregg 著，中文版可读性好，原书名 *Systems Performance*）。
2. 《Systems Performance: Enterprise and the Cloud》（同作者英文第二版，内容更新更全，覆盖容器和云环境）。
3. 《深入理解 Linux 内核》（适合深入学习，初学者可先跳过）。

> 初学者建议：先从《性能之巅》中文版开始，它用大量实际案例解释系统性能，比纯理论书更容易上手。如果英文阅读无障碍，直接看英文第二版更好，因为它覆盖了 eBPF、容器等新内容。

## 2. 第二阶段：网络基础

网络是分布式系统的地基。

必须掌握：

1. TCP/IP。
2. HTTP/1.1、HTTP/2、HTTP/3。
3. 长连接。
4. WebSocket。
5. QUIC。
6. CDN。
7. DNS。
8. TLS（Transport Layer Security）。
9. 连接池。
10. 超时、重试、限流。

做实时语音和 AI 流式产品的人，尤其要重视 WebSocket、HTTP/2 streaming、网络抖动、backpressure 和断线重连。

网络容量的底层逻辑是：每一次通信都要占用连接、带宽、缓冲区和处理能力。短连接有建立成本，长连接有持有成本；重试能提高单次成功率，也可能放大故障；连接池能复用连接，也可能在下游变慢时积压请求。

推荐练习：

1. 用 `curl -w` 观察 DNS、连接、TLS、首包和总耗时。
2. 用 `ss` 或 `netstat` 观察连接状态。
3. 写一个 WebSocket 服务，测 1 万长连接的内存占用。
4. 模拟下游延迟，观察连接池如何耗尽。
5. 用 `tc` 模拟弱网、丢包和延迟。

常见坑：

1. 没有设置超时。
2. 重试没有上限。
3. 所有服务共用一个连接池。
4. WebSocket 心跳过于频繁，浪费带宽。
5. 客户端断线后立即全量重连，形成重连风暴。

网络问题最麻烦的地方在于，它常常表现为应用问题。接口慢了，看起来像代码慢；实际上可能是 DNS 慢、TLS 握手慢、连接池耗尽或者下游 RTT 增加。

## 3. 第三阶段：数据库容量

这一阶段重点学 MySQL、Redis 和 Kafka。

MySQL 要理解：

1. B+ 树索引。
2. Buffer Pool。
3. 事务隔离级别。
4. MVCC（Multi-Version Concurrency Control）。
5. Redo Log。
6. Binlog。
7. 慢查询。
8. 主从复制。
9. 分库分表。

数据库容量的核心不是“表能放多少行”，而是“在目标查询模式下，表增长以后还能不能稳定返回结果”。同样是一亿行表，如果查询都能命中高选择性索引，热数据在 Buffer Pool 中，性能可以很好；如果查询条件混乱、索引设计差、排序和分页扫描大量数据，几百万行也可能很慢。

Redis 要理解：

1. 单线程模型。
2. 数据结构。
3. 内存开销。
4. 淘汰策略。
5. RDB（Redis Database Backup）/AOF（Append Only File）。
6. Cluster。
7. 热 Key 和大 Key。

Kafka 要理解：

1. Topic。
2. Partition。
3. Consumer Group。
4. Offset。
5. Retention。
6. 副本。
7. 顺序写。
8. 消费积压。

推荐练习：

1. 建一张百万级、千万级 MySQL 表，比较不同索引下的查询计划。
2. 观察 Buffer Pool 命中率变化。
3. 构造 Redis 大 Key 和热 Key，观察延迟。
4. 用 Kafka 模拟生产速度大于消费速度，计算积压恢复时间。
5. 做一次小规模数据归档，把热数据和冷数据分开。

常见坑：

1. 只加索引，不看写入成本和索引空间。
2. 使用深分页，越翻越慢。
3. Redis Value 过大，单线程阻塞。
4. Kafka 分区数太少，消费者加不上去。
5. 没有数据保留和归档策略。

数据库容量是架构师的基本功。因为只要系统有状态，状态系统迟早会成为核心约束。

## 4. 第四阶段：性能分析

性能分析是把“感觉”变成“证据”的工具。

必须掌握：

1. flame graph（火焰图：一种可视化工具，横轴表示 CPU 时间占比，纵轴表示调用栈深度）。
2. pprof（Go 语言内置的性能分析工具，Java 对应的是 JFR/async-profiler）。
3. perf（Linux 内核级性能分析工具）。
4. tracing（分布式链路追踪，如 Jaeger、Zipkin、SkyWalking）。
5. eBPF（Linux 内核中的可编程观测框架，可以在不修改代码的情况下观测系统行为）。
6. APM（Application Performance Monitoring，应用性能监控平台）。
7. 慢日志。
8. 系统指标关联分析。

看到延迟升高时，不要急着改代码。先把证据链建立起来：请求在哪个阶段耗时，哪个函数占 CPU，哪个依赖变慢，哪个资源接近上限。

一个好的性能分析流程通常是：

```text
确定用户影响
→ 找到受影响接口
→ 看延迟分布和错误率
→ 看链路追踪
→ 看服务资源
→ 看下游依赖
→ 看热点代码或慢查询
→ 做最小修复
→ 复测验证
```

推荐练习：

1. 给一个 Go 或 Java 服务接入 pprof 或 profiler。
2. 构造 CPU 热点，生成火焰图。
3. 构造内存泄漏，观察堆增长。
4. 给核心接口接入 tracing，看跨服务耗时。
5. 做一次“延迟升高”的故障演练。

常见坑：

1. 没有基线，不知道什么叫异常。
2. 只看单个指标，不看关联。
3. 没有区分 P50 和 P99。
4. 看到热点函数就改，不看业务路径是否真实。
5. 优化后不复测。

性能分析的本质，是建立证据链。没有证据链的优化，常常只是运气。

## 5. 第五阶段：压测体系

这一阶段学习如何设计真实压测。

工具可以从 k6 和 wrk 开始。k6 适合写用户场景，wrk 适合压 HTTP 极限。

你要学会写压测报告，而不只是跑一个命令。

好的压测报告应该告诉团队：系统当前容量是多少，瓶颈是什么，扩容方式是否有效，继续增长会在哪里出问题。

推荐练习：

1. 对一个真实 API 做阶梯压测。
2. 设计混合流量：登录、查询、写入、导出。
3. 记录 P50、P95、P99、错误率和资源利用率。
4. 找到系统拐点。
5. 增加一倍机器后复测，看扩容是否线性。
6. 做 2 到 4 小时稳定性压测，观察内存和延迟漂移。

常见坑：

1. 压测数据完全一样，缓存命中率过高。
2. 压测环境和生产环境差距太大。
3. 只看 QPS，不看失败率和 P99。
4. 不记录系统资源，压完不知道瓶颈在哪。
5. 不设停止条件，压坏共享环境。

压测的目标不是得到一个漂亮数字，而是让团队知道系统边界。

## 6. 第六阶段：分布式系统容量

当单机资源不够时，就进入分布式系统。

必须理解：

1. CAP（注意：CAP 不是说三选二那么简单。CAP 定理说的是：在网络分区（P）发生时，系统只能在一致性（C）和可用性（A）之间选一个。由于网络分区在分布式系统中几乎不可避免，实际决策是在 C 和 A 之间权衡。例如：银行转账选 C，社交动态选 A）。
2. 一致性（强一致、最终一致、因果一致等）。
3. 分片。
4. 复制。
5. 服务发现。
6. Service Mesh。
7. Kubernetes。
8. Elastic Scaling。
9. 限流降级。
10. 多机房灾备。

这一阶段最重要的认识是：分布式系统不是免费午餐。它解决单机容量问题，同时引入一致性、网络分区、运维复杂度和故障传播问题。

例如分库分表可以解决单库容量，但它会改变事务、排序、分页、统计和数据修复方式。Kubernetes 可以提高调度和弹性能力，但它也引入 request/limit、探针、滚动发布、节点资源碎片和网络插件等新问题。

推荐练习：

1. 将一个单体服务拆成两个服务，观察调用链和故障传播。
2. 对数据库按用户 ID 做简单分片。
3. 在 Kubernetes 中设置 request/limit，观察调度和 OOM。
4. 配置 HPA，测试扩容速度。
5. 做一次依赖故障演练，看降级是否生效。

常见坑：

1. 为了“先进”过早引入微服务。
2. 以为加 Kubernetes 就自动高可用。
3. 分片键选择错误，导致热点分片。
4. 限流只在入口做，下游仍被打爆。
5. 多机房只做部署，不做切流演练。

分布式系统的关键不是组件数量，而是边界管理。哪里同步，哪里异步；哪里强一致，哪里最终一致；哪里可降级，哪里必须保证。

## 7. 第七阶段：AI 系统容量

如果你现在做 AI 产品，这一阶段非常重要。

需要重点学习：

1. vLLM（开源推理引擎，支持 continuous batching 和 PagedAttention）。
2. TensorRT-LLM（NVIDIA 优化的推理框架，性能好但依赖 NVIDIA GPU）。
3. Ollama（本地部署开源模型的简单工具，适合入门实验）。
4. KV Cache。
5. Continuous Batching。
6. Batch Scheduling。
7. CUDA（Compute Unified Device Architecture） 基础。
8. GPU 显存计算。
9. Tensor Parallel（模型太大单卡放不下时，跨卡切分）。
10. Pipeline Parallel（按层切分模型到不同 GPU）。
11. TTFT、TPOT、Tokens/s。

> 注意：AI 推理框架发展非常快，vLLM、SGLang、TensorRT-LLM 等工具每几个月都有重大更新。学习时关注核心概念（batching、KV Cache、显存管理），而不是死记某个版本的 API。

AI 推理容量和传统 Web 容量不同。传统 Web 请求通常比较短，CPU 和数据库是主要瓶颈；LLM 请求可能持续几十秒，瓶颈常常在 GPU 显存、KV Cache、batch 调度和 token 生成速度。

推荐练习：

1. 用 Ollama 或 vLLM 部署一个开源模型。
2. 测不同 context length 下的显存占用。
3. 测不同并发下的 TTFT 和 TPOT。
4. 比较流式输出和非流式输出体验。
5. 测 batch size 对吞吐和延迟的影响。
6. 建立一次请求的 token 成本模型。

常见坑：

1. 用 HTTP QPS 衡量 LLM 容量。
2. 只看输出 token，不看输入 token 和长上下文。
3. 忽视 KV Cache 对显存的占用。
4. 没有模型路由，所有请求都走大模型。
5. 没有配额，免费用户也能产生高成本请求。

AI 推理容量将是未来几年非常值钱的工程能力。因为它同时涉及模型、系统、GPU、成本和用户体验。

## 8. 学习节奏建议

如果你是初学者，不要试图同时学完所有东西。可以按这个节奏推进：

| 时间 | 重点 |
| --- | --- |
| 第 1 个月 | Linux 资源、HTTP、基础监控 |
| 第 2 个月 | MySQL 索引、Redis 内存、慢查询 |
| 第 3 个月 | 压测工具、P99、瓶颈定位 |
| 第 4 个月 | Kafka、队列积压、异步架构 |
| 第 5 个月 | Kubernetes、限流降级、弹性扩容 |
| 第 6 个月 | AI 推理容量和成本模型 |

学习容量评估，最好的方法不是看更多概念，而是反复做实验。每做一次实验，你都会对系统边界多一分直觉。

## 9. 一条 30 天入门训练计划

很多初学者的问题不是不努力，而是不知道每天练什么。下面这条 30 天计划，可以作为入门训练。

| 时间 | 任务 | 产出 |
| --- | --- | --- |
| 第 1-3 天 | 学 `top`、`vmstat`、`iostat`、`ss` | 写一页 Linux 指标解释 |
| 第 4-6 天 | 写一个简单 HTTP API | 记录不同并发下 CPU 和延迟 |
| 第 7-9 天 | 给 API 加 MySQL 查询 | 比较有索引和无索引 |
| 第 10-12 天 | 给 API 加 Redis 缓存 | 观察命中率和延迟变化 |
| 第 13-15 天 | 用 k6 做阶梯压测 | 写第一份压测报告 |
| 第 16-18 天 | 构造慢 SQL 和大响应 | 观察 P99 如何变差 |
| 第 19-21 天 | 加入 Kafka 或本地队列 | 观察生产消费速率 |
| 第 22-24 天 | 模拟下游超时和重试 | 观察重试放大效应 |
| 第 25-27 天 | 做一次成本估算 | 算单请求和单用户成本 |
| 第 28-30 天 | 写完整容量评估文档 | 形成闭环报告 |

这 30 天的目标不是掌握所有中间件，而是形成一条完整链路：写服务、压服务、看指标、找瓶颈、做估算、写报告。

## 10. 推荐实验环境

为了降低门槛，可以用 Docker Compose 搭一个最小环境：

> 注意：下面的 Kafka 配置使用 KRaft 模式（不需要 ZooKeeper），这是 Kafka 3.3+ 推荐的部署方式。如果你看到旧教程提到 ZooKeeper，那是 Kafka 的旧架构。

```yaml
services:
  mysql:
    image: mysql:8
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: demo
    ports:
      - "3306:3306"

  redis:
    image: redis:7
    ports:
      - "6379:6379"

  kafka:
    image: bitnami/kafka:latest
    ports:
      - "9092:9092"
    environment:
      KAFKA_CFG_NODE_ID: 1
      KAFKA_CFG_PROCESS_ROLES: broker,controller
      KAFKA_CFG_CONTROLLER_QUORUM_VOTERS: 1@kafka:9093
      KAFKA_CFG_LISTENERS: PLAINTEXT://:9092,CONTROLLER://:9093
      KAFKA_CFG_ADVERTISED_LISTENERS: PLAINTEXT://localhost:9092
      KAFKA_CFG_CONTROLLER_LISTENER_NAMES: CONTROLLER
```

这个环境不适合生产，但适合学习。学习阶段最重要的是把抽象概念变成可观察现象。你亲眼看到 Redis 大 Key 让延迟抖动，看到 Kafka 消费慢导致 lag 增长，看到没有索引的 SQL 扫描大量行，记忆会比看十篇文章更牢。

## 11. 本章检查清单

| 阶段 | 是否掌握 |
| --- | --- |
| 能解释 CPU、内存、IO、网络基础指标 |  |
| 能分析一次 HTTP 请求的资源路径 |  |
| 能设计 MySQL、Redis、Kafka 的基本容量实验 |  |
| 能用 profiler 或 tracing 建立性能证据链 |  |
| 能写一份有瓶颈结论的压测报告 |  |
| 能解释分布式系统扩容带来的新问题 |  |
| 能建立 AI 推理 token 和 GPU 成本模型 |  |

学习路线的本质，是从底层资源走向上层系统。基础越扎实，后面越不容易被术语迷惑。
